```mongodb
// Select the database
use vit

// Create a collection for students
db.createCollection("Student")

// Display all collections
show collections

// Remove the Student collection
db.getCollection("Student").drop()

// Create Employee collection
db.createCollection("Employee")

// Verify available collections
show collections

// Insert employee records
db.Employee.insertOne({
    name: "Udayraj",
    department: "CSE"
})

db.Employee.insertOne({
    name: "Tanna",
    department: "IT"
})

// View inserted documents
db.Employee.find().pretty()

// Add another employee document
db.Employee.insertOne({
    name: "Harsh",
    marks: "90",
    department: "Science"
})

// Display all employee documents
db.Employee.find({}, {
    _id: 1,
    name: 1,
    department: 1,
    marks: 1
}).pretty()

// Switch to the same database again (optional)
use vit
```
